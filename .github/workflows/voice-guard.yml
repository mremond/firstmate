name: Voice guard
run-name: "PR #${{ github.event.pull_request.number }} text - ${{ github.event.action }}"

# The commit-message half of this refusal runs before the first push, inside
# bin/fm-lint.sh. A pull request's title and description have no pre-push
# moment - they do not exist until after the push - so this workflow is the
# earliest firstmate-owned point that can read them.
#
# It matters because of how a squash merge composes public history: the title
# becomes the merge commit's subject and the description can become its body,
# so internal voice in either lands on the default branch under the repository
# owner's name exactly like a leaked commit message would.
#
# "edited" is in the trigger list on purpose: a description rewritten after the
# checks first passed is the one vector the pre-push refusal cannot see.
on:
  pull_request:
    types: [opened, edited, synchronize, reopened]
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: voice-guard-${{ github.event.pull_request.number }}-${{ github.event.action == 'edited' && github.run_id || 'head-change' }}
  cancel-in-progress: true

jobs:
  check:
    name: PR text must be free of internal voice
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      # The title and body are attacker-controlled text, so they reach the
      # check through the environment and stdin, never through run: string
      # interpolation.
      - name: Refuse internal voice in the pull request title and description
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
          PR_BODY: ${{ github.event.pull_request.body }}
        run: printf '%s\n%s\n' "$PR_TITLE" "$PR_BODY" | bin/fm-voice-guard.sh --text -
