# Bearings product validation

Target: fd5a98332357fbb176ab5a06f7fe0ff5452afd70
Base: b84e0e362face25f3dd8945297a3df1320d7668c

## Result

No-go: at a 390px viewport the newly added Delivered threshold label ends at x=415.17px and is partly off-screen. Giving only that header wrapping in the live DOM moves its right edge to 288.66px. The source was not changed. The older Captain's Call heading also overflows; this finding concerns only the new Delivered heading. See narrow-delivered-clipped.png and narrow-counterfactual.txt.

## Runtime exercised

Two disposable FM_HOME directories with hand-authored operational records, real tmux endpoints, real busy-event / PR registration / fleet snapshot / home-summary / Bearings board commands, and an isolated real Lavish server. No fake executables in this live path. Historical delivery timestamps were backdated for age checks; future consumer time used FM_BEARINGS_NOW. The unchanged ledger was generated by the actual producer in this run.

Passed: same-repository ownership; unified needs-you count; Delivered ages and recorded request links; expired delivery promotion; same-PR clock preservation; invalid poll identity rejection; active, failed, blocked, undeclared-done and approval-owed exclusion from Delivered; endpoint-exit retention; prose URLs never promoted to child request links; queued metadata request links; consumer-only threshold validation; overdue retention above the display bound; and fail-closed malformed board input.

The browser selected Leave it on the 23-day nudge, queued it, and sent it through the live Lavish listener. The persisted result uses fm-bearings-nudge.v1; the nudge reader returns its request URL, while answer and reconcile readers emit nothing. The unrelated nudge.foo captain hold stayed open.

## Supporting targeted regressions

Ran the 15 snapshot selectors in targeted-selectors.json, all of tests/fm-bearings-board-render.test.sh, six changed ownership/wait/nudge validator cases from tests/fm-bearings-board.test.sh, and the three delivery-clock cases listed in targeted-selectors.json. All passed. These tests use stubs and are not classified as live. No complete repository suite, lint, formatter, static analysis, publication, PR or CI phase was run.

The completed-no-mistakes-run plus later approval case passed the targeted stubbed regression, but could not be driven against a completed live run: this test-phase agent is explicitly forbidden to create/control another pipeline, and no completed isolated run was supplied. The outer executor can supply an authorized completed run and matching task metadata for that live check.

## Browser environment

chrome-devtools-axi assumed an older page-selection API and the absent Google Chrome app. Computer-use browser discovery had no available browser. Used the already-cached Chromium headless executable, an isolated profile inside the worktree, and the wrapper's real MCP bridge with explicit pageId. Screenshot file writes through MCP refused the evidence path; screenshots were captured from the same browser via its CDP Page.captureScreenshot API directly into the authorized evidence directory. No tools or applications were installed or modified.
