All intentional test setup stayed inside .phase-test in the worktree; evidence was written only in this run evidence directory.
GNU coreutils 9.5 was fetched from https://ftp.gnu.org/gnu/coreutils/coreutils-9.5.tar.xz and built locally, with no system installation. The initial make src/timeout failed before generated headers existed; a normal make built the executable. The first real-timeout setup exposed the entire build directory on PATH, which was corrected to a directory containing only a timeout symlink. A subsequent initialization attempt timed out under host contention; the private driver initialization window was widened from 15 to 60 seconds. This did not change the 150-poll stop budget, the production 5-second cleanup deadline, or any tracked test budget. The final real-timeout scenario passed.
The first live-watcher.py invocation completed five scenarios, then stopped because GNU timeout was not yet built. The final --installed-only invocation supplied the sixth live scenario. runtime-transcript.log consolidates those successful product observations; raw attempts remain alongside it.
The deliberate diagnostic mutation is supplemental fixture evidence, not a live run of the unmodified product. It disables both signal-handler restorations and draining only in disposable copies, then executes the existing 150-poll assertion with a reparented child and grandchild. The original production and test files were never changed.
