missing waiver name
$ bash bin/fm-pr-merge.sh task-x1 https://github.com/example/repo/pull/1 --allow-missing
error: --allow-missing requires a check name

exit=2

duplicate waiver
$ bash bin/fm-pr-merge.sh task-x1 https://github.com/example/repo/pull/1 --allow-missing validate --allow-missing lint
error: --allow-missing may be specified only once

exit=2

equals syntax
$ bash bin/fm-pr-merge.sh task-x1 https://github.com/example/repo/pull/1 --allow-missing=validate
error: --allow-missing requires a separate check name argument

exit=2

GitLab waiver
$ bash bin/fm-pr-merge.sh task-x1 https://gitlab.com/example/repo/-/merge_requests/1 --allow-missing validate
error: --allow-missing does not apply to GitLab, where a merge already requires the head pipeline to have succeeded

exit=2
