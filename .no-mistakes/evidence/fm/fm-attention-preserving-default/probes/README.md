Supervisor-contract probes. Each sN.txt is a synthetic wake/captain event; the "new-*" outputs come from
`claude -p --model sonnet --tools ""` run in the change's worktree root (CLAUDE.md imports AGENTS.md, as in a primary firstmate home).
The "base-*" outputs ran the same prompt against base commit 9bc051ff's AGENTS.md + ask-user-authority skill (base-home/).
s1 empty heartbeat | s2 "what should I work on" | s3 contract-expanding finding escalated by ask-user-authority
s4 in-intent review finding | s5 reproducible CI failure (silence must not hide it) | s6 green PR, no merge authority
